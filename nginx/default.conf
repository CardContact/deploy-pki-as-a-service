# nginx proxy configuration file

server {
    listen 8080;

    location /core-rt {
        resolver 127.0.0.11 valid=10s;
        proxy_pass http://pkiaas-backend-test:8080/rt;
    }

    location / {
        resolver 127.0.0.11 valid=10s;
        proxy_pass http://pkiaas-test:8080;
    }
}
